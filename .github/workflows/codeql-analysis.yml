name: "CodeQL"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  codeql:
    uses: alexdlaird/.github/.github/workflows/codeql.yml@main
    permissions:
      security-events: write
      contents: read
      actions: read
